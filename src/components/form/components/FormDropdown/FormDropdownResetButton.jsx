import styled from "styled-components";

import { Button } from "../../../Button";

const FormDropdownResetButton = styled(Button)`
  position: absolute;
  top: 0;
  bottom: 0;
  right: 3px;
  z-index: 2;
  background-color: inherit;
  line-height: 0;
`;

export { FormDropdownResetButton };
