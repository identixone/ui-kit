import styled from "styled-components";

import { colors } from "../../themes/colors";

export const NoticeHeroIcon = styled.div`
  margin-bottom: 13px;
  line-height: 0;
  color: ${colors.slate};
`;
