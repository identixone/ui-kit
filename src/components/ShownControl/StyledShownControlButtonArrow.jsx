import styled from "styled-components";

import { Button } from "../Button";

const StyledShownControlButtonArrow = styled(Button)`
  width: 36px;
  padding: 0px;
  color: #fff;
  box-shadow: none;

  svg {
    display: block;
  }
`;

export default StyledShownControlButtonArrow;
