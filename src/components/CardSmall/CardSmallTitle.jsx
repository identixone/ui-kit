import styled from "styled-components";

const CardSmallTitle = styled.p`
  margin: 0;
  font-size: 12px;
  line-height: 11px;

  &:not(:last-child) {
    margin-bottom: 12px;
  }
`;

export { CardSmallTitle };
