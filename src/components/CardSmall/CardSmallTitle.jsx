import styled from "styled-components";

const CardSmallTitle = styled.p`
  margin: 0;
  font-size: 12px;
  font-weight: 600;

  &:not(:last-child) {
    margin-bottom: 9px;
  }
`;

export { CardSmallTitle };
