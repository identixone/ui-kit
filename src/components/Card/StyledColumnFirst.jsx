import styled from "styled-components";
import StyledColumn from "./StyledColumn";

const StyledColumnFirst = styled(StyledColumn)`
  margin-left: 170px;
  width: 305px;
`;

export default StyledColumnFirst;
