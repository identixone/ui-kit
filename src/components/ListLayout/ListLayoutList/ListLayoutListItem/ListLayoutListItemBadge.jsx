import styled from "styled-components";

export const ListLayoutListItemBadge = styled.span`
  padding: 3px 8px;
  border-radius: 22.5px;
  color: #ffffff;
  background-color: ${({ color }) => color};
  font-size: 13px;
  line-height: 1.08;
`;
