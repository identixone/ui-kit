import styled from "styled-components";

import { Spinner } from "../../Spinner/index";

export const ListLayoutListSpinner = styled(Spinner)`
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
`;
