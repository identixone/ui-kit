import styled from "styled-components";

export const StyledListLayoutList = styled.ul`
  padding-left: 0;
  margin: 0;
  list-style-type: none;
  min-height: 350px;

  &:not(:only-child) {
    margin-bottom: 27px;
  }
`;
