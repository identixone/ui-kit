import styled from "styled-components";
import { StyledEntryCardContainer } from "../EntryCardContainer";

const StyledIdEntry = styled(StyledEntryCardContainer)`
  height: 105px;
`;

export default StyledIdEntry;
