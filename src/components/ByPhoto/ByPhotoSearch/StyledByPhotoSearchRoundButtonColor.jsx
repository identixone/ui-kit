import styled from "styled-components";
import StyledPlaceColor from "./StyledPlaceColor";

const StyledByPhotoSearchRoundButtonColor = styled(StyledPlaceColor)`
  padding: 1px 12px;
  border-radius: 15px;
  cursor: pointer;
`;

export default StyledByPhotoSearchRoundButtonColor;
