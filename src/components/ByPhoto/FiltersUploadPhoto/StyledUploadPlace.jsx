import styled from "styled-components";

const StyledUploadPlace = styled.div`
  box-sizing: border-box;
  transition: none;
  user-select: none;

  margin-bottom: 22px;

  position: relative;
  overflow: hidden;
  outline: none;
}
`;

export default StyledUploadPlace;
