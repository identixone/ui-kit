import styled from "styled-components";

import { colors } from "../../../../../style";

const DatePickerRelativePickerSelectLabel = styled.label`
  width: 64px;
  font-size: 12px;
  font-weight: 600;
  color: ${colors.darkBlack};
`;

export { DatePickerRelativePickerSelectLabel };
