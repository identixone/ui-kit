import styled from "styled-components";

import { colors } from "../../../style";

const InfoCardFieldLabel = styled.span`
  font-weight: 600;
  font-size: 12px;
  line-height: 16px;
  color: ${colors.darkBlack};
  margin-right: 8px;
`;

export { InfoCardFieldLabel };
