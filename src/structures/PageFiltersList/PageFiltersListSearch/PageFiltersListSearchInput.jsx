import styled from "styled-components";

import { Search } from "@idx/ui/components";

const PageFiltersListSearchInput = styled(Search).attrs({
  inputPlaceholder: "Start enter name...",
})``;

export default PageFiltersListSearchInput;
