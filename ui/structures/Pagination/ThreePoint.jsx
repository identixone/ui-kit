import styled from "styled-components";

const ThreePoint = styled.span`
  width: 1px;
  height: 1px;
  position: absolute;
  border: 0;
  clip: rect(0 0 0 0);
  display: inline-block;
  margin: -1px;
  overflow: hidden;
  padding: 0;

  width: auto;
  height: auto;
  position: static;
  clip: none;
  margin: 0;
  overflow: visible;

  opacity: 1;
  visibility: visible;
`;

export default ThreePoint;
