import styled from "styled-components";

import StyledColumn from "./StyledColumn.jsx";

const StyledIdColumn = styled(StyledColumn)`
  margin-left: 60px;
  margin-top: 9px;
`;

export default StyledIdColumn;
