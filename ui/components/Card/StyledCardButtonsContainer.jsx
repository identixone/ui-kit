import styled from "styled-components";

const StyledCardButtonsContainer = styled.div`
  position: absolute;
  display: flex;
  flex-direction: column;
  top: 0;
  right: 0;
`;

export default StyledCardButtonsContainer;
