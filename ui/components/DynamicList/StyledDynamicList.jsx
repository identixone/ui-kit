import styled from "styled-components";

const StyledDynamicList = styled.ul`
  list-style-type: none;
  padding-left: 0;
  margin: 0;
  position: relative;
  overflow: hidden;
`;

export default StyledDynamicList;
